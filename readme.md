# SignPDF Project

## Overview
This project is designed to handle PDF signing functionalities using Python.

## Installation
To install the required dependencies, run the following command:

```bash
pip install PyPDF2 reportlab